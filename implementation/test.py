# alfa = pow(fin_temp / T_0, 1 / -9900)
import random

import numpy as np

var = [1]*3
print(var)

listss = [1, 2, 3, 4]

out = random.choices([x for x in range(len(listss))], weights=[90, 2, 3, 5])[0]
print(out)
